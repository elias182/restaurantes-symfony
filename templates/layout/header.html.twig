{# templates/layout/header.html.twig #}
<head>
    <!-- Otros elementos head -->
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/header/styles.css') }}">
    <!-- Otros elementos head -->
</head>
<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/"><img src="{{ asset('img/Captura_de_pantalla_2024-02-12_134511-removebg-preview.png') }}"></a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent" >
                <ul class="navbar-nav"> 
                    <li class="nav-item funcion">
                        <a class="nav-link" href="{{ path('app_productos_index') }}">Productos</a>
                    </li>
                    <li class="nav-item funcion">
                        <a class="nav-link" href="#">Nosotros</a>
                    </li>
                    <li class="nav-item funcion">
                        <a class="nav-link" href="#">Contacto</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item">
                            <span class="nav-link">Bienvenido, {{ app.user.email }}</span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link logout" href="{{ path('app_logout') }}">Cerrar sesión</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link register" href="{{ path('app_register') }}">Registrarse</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link login" href="{{ path('app_login') }}">Iniciar sesión</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>