<form method="post" action="{{ path('app_restaurantes_delete', {'codRes': restaurante.codRes}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurante.codRes) }}">
    <button class="btn">Delete</button>
</form>
