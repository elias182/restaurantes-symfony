<form method="post" action="{{ path('app_pedidos_productos_delete', {'id': pedidos_producto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pedidos_producto.id) }}">
    <button class="btn">Delete</button>
</form>
