{% extends 'base.html.twig' %}

{% block title %}PedidosProductos{% endblock %}

{% block body %}
    <h1>PedidosProductos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodPedProd</th>
                <td>{{ pedidos_producto.codPedProd }}</td>
            </tr>
            <tr>
                <th>Unidades</th>
                <td>{{ pedidos_producto.unidades }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pedidos_productos_index') }}">back to list</a>

    <a href="{{ path('app_pedidos_productos_edit', {'codPedProd': pedidos_producto.codPedProd}) }}">edit</a>

    {{ include('pedidos_productos/_delete_form.html.twig') }}
{% endblock %}
