{% extends 'base.html.twig' %}

{% block title %}PedidosProductos index{% endblock %}

{% block body %}
    <h1>PedidosProductos index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Unidades</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pedidos_producto in pedidos_productos %}
            <tr>
                <td>{{ pedidos_producto.id }}</td>
                <td>{{ pedidos_producto.unidades }}</td>
                <td>
                    <a href="{{ path('app_pedidos_productos_show', {'id': pedidos_producto.id}) }}">show</a>
                    <a href="{{ path('app_pedidos_productos_edit', {'id': pedidos_producto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_pedidos_productos_new') }}">Create new</a>
{% endblock %}
