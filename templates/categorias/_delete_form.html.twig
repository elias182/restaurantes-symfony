<form method="post" action="{{ path('app_categorias_delete', {'codCat': categoria.codCat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoria.codCat) }}">
    <button class="btn">Delete</button>
</form>
