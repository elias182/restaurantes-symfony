<form method="post" action="{{ path('app_pedidos_delete', {'id': pedido.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este pedido?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pedido.id) }}">
    <button type="submit" class="btn btn-danger" style="margin-top: 10px;">Eliminar</button>
</form>
